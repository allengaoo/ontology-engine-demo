# 第一阶段：无外部依赖（Python 标准库即可）
# python_requires>=3.8

# 第二阶段（可选）：真实 LLM 路径
# pip install openai          # OpenAI SDK（兼容 OpenAI / DeepSeek / 其他兼容接口）
# pip install python-dotenv   # 自动加载 .env 文件（未安装时代码会内置简单解析）

# 开发工具（可选）
# pip install pytest
# pip install black
# pip install pyyaml  # 若系统 Python 未自带 yaml（多数环境已内置）
