# CodingAgent — 编码智能体（CA）
# code-arch + domain，按 Unit fresh context

[memory]
domains           = ["code-arch", "domain"]
tiers             = ["hot"]
read_layers       = ["critical", "rule", "pattern"]
write_layers      = []
budget_multiplier = 0.8

[concept_hints]
keywords = ["architecture", "meeting_order", "fastapi", "react"]
