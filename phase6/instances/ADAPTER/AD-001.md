---
id: AD-001
object_type: DecisionRecord
title: 序列化失败：改用显式 Schema 注册
layer: ADAPTER
tier: warm
tags: [avro, serialization, kafka]
confidence: 0.85
schema_version: 1
decision: 启动时强制 Schema Registry 握手；失败则拒绝启动，不静默降级
about_concepts: [avro, serialization, schema-registry]
derived_from: [task-2026-03-avro-fix]
cites_files: [src/adapter/kafka_producer.py]
---

## 背景

Avro 静默失败：生产者未注册 Schema，消费端反序列化返回空对象。

## 决策

启动时强制 Schema Registry 握手；失败则拒绝启动，不静默降级。

## 备选

仅日志告警（已否决：线上难发现）。
